# Runtime dependencies (also declared in pyproject.toml)
faster-whisper>=1.0
openai>=1.0
sounddevice>=0.4
soundfile>=0.12
numpy>=1.24
pynput>=1.7

# GTK3 Python bindings — install via apt, NOT pip:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
#
# After creating your venv, run this once to make the system gi package visible:
#   echo "/usr/lib/python3/dist-packages" \
#     > .venv/lib/python3.*/site-packages/system-gi.pth
